# Twitter Daily Poster (1 твит в день)

Этот скрипт **публикует один твит за запуск**. Настройте планировщик ОС (Windows Task Scheduler / cron), чтобы запускать его **раз в день** — и у вас будет автопостинг.

## 📁 Что внутри
- `main.py` — основной скрипт.
- `requirements.txt` — зависимости.
- `.env.example` — шаблон переменных окружения (переименуйте в `.env`).
- `content/ideas_ru.txt` — идеи для постов (по одной строке на твит). Можно использовать плейсхолдер `{topic}`.
- `data/state.json` — состояние (индекс следующего твита, последний постинг). Создаётся автоматически.

## 🚀 Быстрый старт
1. Установите Python 3.10+
2. Склонируйте папку или распакуйте архив.
3. В консоли перейдите в папку проекта и установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
4. Создайте файл `.env` (скопируйте из `.env.example`) и заполните ключи API X (Twitter).
   - Понадобятся: `TWITTER_API_KEY`, `TWITTER_API_SECRET`, `TWITTER_ACCESS_TOKEN`, `TWITTER_ACCESS_SECRET`.
   - У аккаунта должен быть **доступ на запись** (платный план X API).
5. Отредактируйте `content/ideas_ru.txt` — по одной идее в строке. Можно использовать `{topic}` из `.env`.
6. Протестируйте **однократный запуск** (создаст твит и сдвинет очередь идей):
   ```bash
   python main.py
   ```

## ⏰ Автозапуск раз в день

### Вариант A: Windows (Task Scheduler)
1. Откройте **Планировщик заданий** → **Создать задачу…**
2. Вкладка **Триггеры** → **Создать…** → *По расписанию* → **Ежедневно** → Время запуска (например, 09:30).
3. Вкладка **Действия** → **Создать…**
   - Действие: *Запуск программы*
   - Программа/скрипт: `python`
   - Добавить аргументы: `main.py`
   - Рабочая папка: *путь до папки проекта*
4. Вкладка **Общие**: отметьте **Выполнять с наивысшими правами** (по желанию).
5. Готово. Задача будет запускать скрипт ежедневно и публиковать **один** твит.

### Вариант B: Linux/macOS (cron)
```bash
crontab -e
# Каждый день в 09:30 по локальному времени
30 9 * * * cd /ПУТЬ/К/ПРОЕКТУ && /usr/bin/python3 main.py >> cron.log 2>&1
```

> Скрипт сам позаботится о том, чтобы брать **следующую** строку из `ideas_ru.txt`. Когда идеи закончатся, порядок перемешается и цикл начнётся заново.

## ⚙️ Переменные окружения (.env)
- `TWITTER_API_KEY`, `TWITTER_API_SECRET`, `TWITTER_ACCESS_TOKEN`, `TWITTER_ACCESS_SECRET` — ключи X API.
- `TOPIC` — строка, которая подставляется в контент вместо `{topic}`.
- `TIMEZONE` — таймзона для дат и окон запуска (например, `Europe/Riga`).
- `ADD_DATE` — `true/false`: добавлять ли дату в конец поста.
- `ADD_HASHTAG` — добавит единый хэштег (без #) к постам.
- `RUN_WINDOW_START` и `RUN_WINDOW_END` — локальное окно времени, в которое **разрешено** публиковать. Если скрипт запущен вне окна — пост пропускается.

## ❗ Важно
- Для публикации через Tweepy используется **OAuth 1.0a** (v1.1 `update_status`). Нужен **запись-доступ** в плане X API.
- Следите, чтобы один и тот же день не запускал скрипт много раз (или используйте окно `RUN_WINDOW_*`).

## 🧪 Полезные команды
```bash
# Проверка зависимостей
pip install -r requirements.txt

# Пробный твит (один раз)
python main.py
```

Удачного автопостинга! 🐦
